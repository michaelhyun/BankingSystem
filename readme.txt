Michael Hyun

Student ID: 00928299

The GUI is pretty intuitive, but the only thing is that I print the Reports in the console, so any report will show up on the console if you click the button. Invalid Withdraws and transfers will not process, but I did not include a message that says if it failed. To login as admin, use ID = 0 and PIN = 0. Everything should work!